# Used by docker-compose.yml for the db service.
# Copy to .env at the project root (or export these) before running
# `docker compose up`. Change the passwords before deploying anywhere real.
DB_ROOT_PASSWORD=changeme_root
DB_NAME=sipsugy
DB_USER=sipsugy
DB_PASSWORD=changeme
