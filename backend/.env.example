# Server
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# Database (tier 3)
# Leave these unset/commented to run the API on seed data + an in-memory
# order store — useful for testing the backend before the DB tier exists.
# Once the database is ready, fill these in and restart:
# DB_HOST=
# DB_PORT=3306
# DB_USER=
# DB_PASSWORD=
# DB_NAME=sipsugy
